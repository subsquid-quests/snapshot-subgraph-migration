"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.formatHead = exports.getItemsCount = exports.timeInterval = exports.getOrGenerateSquidId = void 0;
function getOrGenerateSquidId() {
    return process.env.SQUID_ID || `gen-${randomString(10)}`;
}
exports.getOrGenerateSquidId = getOrGenerateSquidId;
function randomString(len) {
    let chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
    let result = '';
    for (let i = 0; i < len; i++) {
        result += chars[Math.floor(Math.random() * chars.length)];
    }
    return result;
}
function timeInterval(seconds) {
    if (seconds < 60) {
        return Math.round(seconds) + 's';
    }
    let minutes = Math.ceil(seconds / 60);
    if (minutes < 60) {
        return minutes + 'm';
    }
    let hours = Math.floor(minutes / 60);
    minutes = minutes - hours * 60;
    return hours + 'h ' + minutes + 'm';
}
exports.timeInterval = timeInterval;
function getItemsCount(blocks) {
    let count = 0;
    for (let block of blocks) {
        for (let key in block) {
            let val = block[key];
            if (Array.isArray(val)) {
                count += val.length;
            }
        }
    }
    return count;
}
exports.getItemsCount = getItemsCount;
function formatHead(head) {
    return `${head.height}#${head.hash.slice(2, 10)}`;
}
exports.formatHead = formatHead;
//# sourceMappingURL=util.js.map